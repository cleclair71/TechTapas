<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>TechTapas</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="./css/stylesheet.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a href="/" class="navbar-brand"><img src="./assets/navlogo.png" height="100" alt="TECHTAPAS"></a>
            <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                {{#if loggedIn}}
                <div class="navbar-nav">
                    <a href="/" class="nav-item nav-link active">Home</a>
                    <a href="/dashboard" class="nav-item nav-link">Dashboard</a>
                    <a href="/dashboard/create" class="nav-item nav-link">Create Post</a>
                    <a href="#" class="nav-item nav-link">Contact</a>
                </div>
                <div class="navbar-nav ms-auto">
                    <a href="/logout" class="nav-item nav-link">Logout</a>
                </div>
                {{else}}
                <a href="/" class="nav-item nav-link active">Home</a>
                <a href="/login" class="nav-item nav-link">Login</a>
                {{/if}}
            </div>
        </div>
    </nav>

<div class="container my-5 border rounded p-4 bg-dark text-light">
  <div class="d-flex justify-content-between align-items-center">
    <h2 class="fw-bold text-primary mb-0">Dashboard: Your Posts</h2>
    <a href="./main.handlebars" class="btn btn-outline-primary btn-sm">Create Post</a>
  </div>
  <hr class="bg-light">
  {{#if posts.length}}
    <div class="row mt-4">
      {{#each posts as |post|}}
        <div class="col-lg-6 mb-4">
          {{> post-info post}}
          <div class="d-flex justify-content-end mt-3">
            <a href="/dashboard/editPost/{{post.id}}" class="btn btn-outline-warning btn-sm me-3">Edit Post</a>
          </div>
        </div>
      {{/each}}
    </div>
  {{else}}
    <div class="text-center mt-5">
      <h6 class="fw-bold mb-3">You do not currently have any posts.</h6>
    </div>
  {{/if}}
</div>